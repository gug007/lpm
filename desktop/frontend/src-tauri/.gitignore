# Rust build output
/target

# Staged CLI sidecar binaries (built by scripts/build-cli.sh before each
# tauri build; per-arch, target-triple-suffixed). Build artifacts, never committed.
/binaries

# Tauri-generated capability schemas
/gen/schemas

# `tauri icon` mobile / Windows-Store output — this app is macOS-only
/icons/android
/icons/ios
/icons/Square*Logo.png
/icons/StoreLogo.png
