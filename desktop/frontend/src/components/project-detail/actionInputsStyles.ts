// Shared field styling for the action-questions editor, so the row and its
// settings panel stay visually identical while living in separate modules.
export const FIELD_CLASS =
  "w-full rounded-md border border-transparent bg-[var(--bg-secondary)] px-2.5 py-1.5 text-[12px] text-[var(--text-primary)] outline-none transition placeholder:text-[var(--text-muted)] focus:border-[var(--accent-cyan)]";
