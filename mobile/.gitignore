# Generated by `xcodegen generate` — not checked in (regenerate from project.yml).
LpmMobile.xcodeproj/
Info.plist
NotificationService-Info.plist
# Entitlements are generated from project.yml (source of truth), like Info.plist.
LpmMobile.entitlements
LpmMobileNotificationService.entitlements

# Local signing team override for run.sh (personal; never committed).
.team

# Swift/Xcode build artifacts
.build/
build/
DerivedData/
*.xcuserstate
.swiftpm/
